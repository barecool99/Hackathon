{% extends 'bootstrap/base.html' %}

{% block head %}
    {{ super () }}

    <head>
        <link href = "{{ url_for('static', filename='css/stylesheet.css') }}" type = "text/css" rel = "stylesheet" />

        <title>
            {% block title %}

                {% if title %}
                    {{ title }}
                {% else %}
                    Hackathon Project
                {% endif %}

            {% endblock %}
        </title>
    </head>
{% endblock %}



{% block navbar %}

    <nav class="navbar navbar-default">
        <div class="container">
            <div class="navbar-header">

                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>

                <a class="navbar-brand" href="">Team Dank Imposters</a>
            </div>

            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">

                    <li><a href="{{ url_for('index') }}">Home</a></li>
                    <li><a href="{{ url_for('diagnose') }}">Diagnose</a></li>
                    <li><a href="{{ url_for('mlAccuracy') }}">Machine Learning Accuracy</a></li>

                </ul>

                <ul class="nav navbar-nav navbar-right">

                        <li><a href="">Login</a></li>
                        <li><a href="">Logout</a></li>

                </ul>

            </div>
        </div>
    </nav>

{% endblock %}


{% block content %}



{% endblock %}